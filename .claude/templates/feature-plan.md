# <Feature name>

## User outcome

One observable result for the user.

## Decisions

- Data ownership:
- UI entry point:
- Failure or offline behavior:
- Compatibility or migration:

## Checklist

1. Add a focused failing test when the behavior is testable.
2. Implement the smallest change and run the focused test.
3. Run `./gradlew :app:assembleDebug` and the applicable device path.
4. Record the handoff state and verification in `CURRENT_STATE.md`.

## Acceptance checks

- Exact test command:
- Exact device interaction:
- Expected offline or failure behavior:
